{
  "name": "portfolio-website",
  "author": "Jonathan Kido",
  "version": "3.0.0",
  "scripts": {
    "start": "concurrently -n backend,frontend,admin -c cyan,magenta,yellow \"npm start --prefix backend\" \"npm start --prefix frontend\" \"npm start --prefix admin\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend && npm run build --prefix admin",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}